#ifndef __stork_persona_HPP__
#define __stork_persona_HPP__

namespace stork {
  namespace persona {
    /**
     * A persona is a unique identity.
     *
     * It is identified by the sha256 hash of its public key.
     *
     * A persona has 'human-readable' information associated with it
     * as well.
     */
    class Persona {
    };
  }
}

#endif
